{
  "distance_influence": 15,
  "priority": [
    { "if": "urban_density == CITY", "multiply_by": "0.5" },
    { "else_if": "urban_density == RESIDENTIAL", "multiply_by": "0.9" },
    { "if": "road_class == PRIMARY", "multiply_by": "0.6" },
    { "else_if": "road_class == SECONDARY", "multiply_by": "0.9" },
    { "if": "road_class == CYCLEWAY", "multiply_by": "1.1" },
    { "else_if": "(road_class == FOOTWAY || road_class == PATH) && (bike_road_access == YES || bike_road_access == DESIGNATED)", "multiply_by": "1.1" }
  ],
  "speed": [
    { "if": "(road_class == FOOTWAY || road_class == PATH) && (bike_road_access == YES || bike_road_access == DESIGNATED)", "multiply_by": "1.5" }
  ]
}
